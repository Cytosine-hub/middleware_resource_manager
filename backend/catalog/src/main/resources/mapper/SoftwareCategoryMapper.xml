<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.middleware.manager.repository.SoftwareCategoryMapper">
    <select id="findAllByOrderByNameAsc" resultType="SoftwareCategory">SELECT * FROM software_categories ORDER BY name ASC</select>
    <select id="existsByNameIgnoreCase" resultType="boolean">SELECT COUNT(*) > 0 FROM software_categories WHERE LOWER(name) = LOWER(#{name})</select>
    <select id="findById" resultType="SoftwareCategory">SELECT * FROM software_categories WHERE id = #{id}</select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">INSERT INTO software_categories (name, created_at, updated_at) VALUES (#{name}, #{createdAt}, #{updatedAt})</insert>
    <delete id="deleteById">DELETE FROM software_categories WHERE id=#{id}</delete>
</mapper>
