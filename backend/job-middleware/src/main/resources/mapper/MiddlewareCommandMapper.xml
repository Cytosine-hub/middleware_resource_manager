<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.middleware.manager.repository.MiddlewareCommandMapper">
    <select id="findBySoftwareTypeIdOrderBySortOrderAsc" resultType="MiddlewareCommand">SELECT * FROM middleware_commands WHERE software_type_id = #{softwareTypeId} ORDER BY sort_order ASC</select>
    <select id="findAllByOrderBySoftwareTypeIdAscSortOrderAsc" resultType="MiddlewareCommand">SELECT * FROM middleware_commands ORDER BY software_type_id ASC, sort_order ASC</select>
    <select id="findBySoftwareTypeIdsOrderBySortOrderAsc" resultType="MiddlewareCommand">
        SELECT * FROM middleware_commands WHERE software_type_id IN
        <foreach collection="softwareTypeIds" item="softwareTypeId" open="(" separator="," close=")">
            #{softwareTypeId}
        </foreach>
        ORDER BY software_type_id ASC, sort_order ASC
    </select>
    <select id="findDistinctSoftwareTypeIds" resultType="long">SELECT DISTINCT software_type_id FROM middleware_commands</select>
    <select id="findById" resultType="MiddlewareCommand">SELECT * FROM middleware_commands WHERE id = #{id}</select>
    <select id="findBySoftwareTypeIdAndCommandFormat" resultType="MiddlewareCommand">SELECT * FROM middleware_commands WHERE software_type_id = #{softwareTypeId} AND command_format = #{commandFormat} LIMIT 1</select>
    <select id="count" resultType="long">SELECT COUNT(*) FROM middleware_commands</select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">INSERT INTO middleware_commands (software_type_id, name, command, command_format, brief_description, detailed_description, categories, sort_order) VALUES (#{softwareTypeId}, #{name}, #{command}, #{commandFormat}, #{briefDescription}, #{detailedDescription}, #{categories}, #{sortOrder})</insert>
    <update id="update">UPDATE middleware_commands SET software_type_id=#{softwareTypeId}, name=#{name}, command=#{command}, command_format=#{commandFormat}, brief_description=#{briefDescription}, detailed_description=#{detailedDescription}, categories=#{categories}, sort_order=#{sortOrder} WHERE id=#{id}</update>
    <delete id="deleteById">DELETE FROM middleware_commands WHERE id=#{id}</delete>
</mapper>
